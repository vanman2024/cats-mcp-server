# CATS MCP Server dependencies
#
# Generated from pyproject.toml [project.dependencies] — keep the two in sync.
# A test asserts they match.
#
# Install with:
#   uv pip install -r requirements.txt --prerelease=allow
#   pip install -r requirements.txt
#
# fastmcp 4 is a prerelease. Every other pin is exact so that the prerelease
# surface stays limited to fastmcp alone — a loose specifier lets uv resolve
# that package to a prerelease too (httpx went to 1.0.dev3 this way).
#
# FastMCP 4 uses httpx2, not httpx. Do not add httpx back.

fastmcp==4.0.0b2
httpx2==2.10.0
pydantic==2.13.4
python-dotenv==1.2.2
