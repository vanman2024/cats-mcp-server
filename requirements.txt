# FastMCP CATS API Server Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
fastmcp>=2.0.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Development dependencies (install with: pip install -r requirements.txt -r requirements-dev.txt)
