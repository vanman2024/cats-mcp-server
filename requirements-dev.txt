# Development dependencies
#
# Generated from pyproject.toml [project.optional-dependencies].dev — keep in sync.
#
# Install with:
#   uv pip install -r requirements.txt -r requirements-dev.txt --prerelease=allow

pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.2
